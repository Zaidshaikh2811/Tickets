apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-mongo
  labels:
    app: orders-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orders-mongo
  template:
    metadata:
      labels:
        app: orders-mongo
    spec:
      containers:
      - name: orders-mongo
        image: mongo:latest
        ports:
        - containerPort: 27017
        
---
apiVersion: v1
kind: Service
metadata:
  name: orders-mongo-srv
  labels:
    app: orders-mongo
spec:
  ports:
  - port: 27017
    protocol: TCP
    name: db
    targetPort: 27017
  selector:
    app: orders-mongo
    